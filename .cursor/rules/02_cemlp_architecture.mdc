---
description:
globs:
alwaysApply: false
---
# Архитектура CatEmbMLP

[CatEmbMLP](mdc:models/nn/cemlp.py) - это нейросетевая архитектура, специально разработанная для работы с табличными данными, содержащими как категориальные, так и числовые признаки.

## Основные особенности

- **Эмбеддинги для категориальных признаков** - автоматическое преобразование категориальных признаков в векторные представления
- **Динамическое определение размера эмбеддингов** - опциональная возможность автоматически определять размер эмбеддингов в зависимости от количества уникальных значений
- **Батч-нормализация** - для стабилизации и ускорения обучения
- **Возможность использования Self-Attention** - опциональный механизм внимания для моделирования взаимодействий между признаками

## Оценщики (Estimators)

Модель представлена тремя оценщиками, совместимыми с интерфейсом scikit-learn:

- [CatEmbMLPBinary](mdc:models/estimators/cemlp_estimator.py) - для задач бинарной классификации
- [CatEmbMLPMulticlass](mdc:models/estimators/cemlp_estimator.py) - для задач многоклассовой классификации
- [CatEmbMLPRegressor](mdc:models/estimators/cemlp_estimator.py) - для задач регрессии

## Процесс обработки данных

1. Числовые признаки опционально масштабируются с использованием различных методов (стандартизация, минмакс, квантильная трансформация)
2. Категориальные признаки преобразуются в числовые коды и далее в эмбеддинги
3. Числовые признаки и эмбеддинги конкатенируются
4. Полученный вектор проходит через MLP с опциональными слоями нормализации и дропаута
